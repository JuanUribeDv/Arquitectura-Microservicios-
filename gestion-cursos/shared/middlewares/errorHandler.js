/**
 * errorHandler.js
 * ----------------
 * Middleware centralizado para manejar errores del backend.
 *
 * Función esperada:
 * - Capturar errores de ejecución
 * - Responder con código de estado apropiado
 * - Registrar mensajes de error
 */

// Ejemplo base:
// export function errorHandler(err, req, res, next) {
//   console.error(err);
//   res.status(500).json({ message: 'Error interno del servidor' });
// }

console.log('Handler de errores cargado');
